PHASE 2 RESUME STATE (updated after instant-view RENDER batch)
Branch: feat/upstream-backport-bulk  HEAD: ec6abea4d (+ pending test commit)
Build: docker 'tdlib-build'; clean-regen auto/ on .tl change; cherry-pick -Xno-renames; tdcore then run_all_tests; test filter SUBSTRING.

EPICS DONE (Phase2): live-location(11); chat-join(5/15 present); search-filter(6); webbrowser(12/5 present);
  instant-view RENDER: RichText sub-batch(8 picks of 23) + PageBlock RENDER sub-batch(5 picks of 26: ca67bf738
  4f3676d76 8df09c1ba 39d166296 108b33f15). Contracts: chatjoin 5/5, search 4/4, webbrowser 3/3, instant-view 2/2.

*** DEFERRED — rich-message SEND/content cluster (HIGH-RISK, non-mission, fork fully behind) ***
  726b15907 (get_input_page_block) + clone() infra: clone() impls come from 48f727bb1 "Implement RichMessage::clone()"
  which is part of the messageRichMessage CONTENT-TYPE epic the fork lacks entirely (no RichMessage.cpp). Applying
  726b15907 alone makes all ~33 WebPageBlock subtypes ABSTRACT (clone()=0 pure virtual w/o impls). Tightly coupled:
  get_input_page_block <-> clone <-> RichMessage type <-> input types (inputAnimation/Audio/Document/Photo/Video).
  ALSO deferred: aa2d3178b (pageBlockAnimation.has_spoiler — entangled with animation clone/get_input_page_block).
  Decision pending from user: force the whole SEND cluster (~30 commits, manual reconstruction, serialization risk) vs defer.

REMAINING: rich-message SEND/content(~30, deferred pending decision); poll-media/W3-P(22, security-relevant);
  misc-functional(12); NONFUNC(19 skip).
RULES: prompt (2).md — fork-safety > upstream-purity; semantic status; tests before pick; preserve fail-closed.
