PHASE 2 RESUME STATE (rules-compliant; updated after webbrowser)
Branch: feat/upstream-backport-bulk  HEAD: 58e73b385 (webbrowser epic complete) + pending test commit
Build env: docker 'tdlib-build'; clean-regen auto/ before build on .tl change; cherry-pick -Xno-renames;
  build tdcore then run_all_tests, --parallel 3; test filter is SUBSTRING (no '*').
Base for diff: feat/adaptive-runtime-profile-rotation

EPICS DONE (Phase 2): live-location(11) GREEN; chat-join(5 picked/15 present) GREEN;
  search-filter(6) GREEN; webbrowser(12 picked/5 present) GREEN.
  Resolved SHAs so far: 54/145. Contract tests: chatjoin 5/5, search 4/4, webbrowser 3/3 (RED->GREEN).

REMAINING (big interconnected cluster — needs careful scoping):
  RichText/PageBlock/WebPageBlock infra + messageRichMessage content + poll-media/W3-P.
  NOTE: grep 'WebPageBlock|PageBlock|RichText|pageBlock' over intake range matched 52 commits — the
  resume split (WebPageBlock 16 / rich-message 53) is artificial; they share RichText infrastructure.
  poll-media/W3-P (~30) is separate and HARDEST (entangled with fork poll schema W3-P hardening).
RULES: prompt (2).md — semantic status per commit; tests BEFORE pick (red->green); fail-closed preserved;
  small waves; no curl; build serially; -Xno-renames; preserve fork form.
